import fs from 'node:fs';

const index=fs.readFileSync('frontend/index.html','utf8');
const workspace=fs.readFileSync('frontend/workspace.js','utf8');
const active=[...index.matchAll(/<script[^>]+src="([^"]+\.js)(?:\?[^\"]*)?"/g)].map(m=>m[1]);
const bundle=active.map(p=>fs.readFileSync('frontend/'+p,'utf8')).join('\n');
const displayFront=fs.readFileSync('frontend/display-depth-v42.js','utf8');
const displayBack=fs.readFileSync('backend/release-v42-display-safety.mjs','utf8');
const source41=fs.readFileSync('frontend/source-depth-v41.js','utf8');
const live71=fs.readFileSync('frontend/live-monitoring-v71.js','utf8');
const quality72=fs.readFileSync('frontend/hmi-quality-v72.js','utf8');
const qualityBack44=fs.readFileSync('backend/release-v44-quality-unit.mjs','utf8');
const process73=fs.readFileSync('frontend/process-capability-v73.js','utf8');
const processBack59=fs.readFileSync('backend/release-v59-process-capability.mjs','utf8');
const governance19=fs.readFileSync('backend/release-v19-governance.mjs','utf8');
const uatView=fs.readFileSync('frontend/uat-release-view-v17.js','utf8');
const uatEdit=fs.readFileSync('frontend/uat-release-edit-v17.js','utf8');
const entry63=fs.readFileSync('backend/release-v63-entry-semantics.mjs','utf8');
const form65=fs.readFileSync('frontend/form-semantics-v65.js','utf8');
const support74=fs.readFileSync('frontend/support-evidence-v74.js','utf8');
const support21=fs.readFileSync('backend/release-v21-support.mjs','utf8');
const kpi45=fs.readFileSync('backend/release-v45-kpi-semantics.mjs','utf8');
const capacity75=fs.readFileSync('frontend/capacity-utilization-v75.js','utf8');
const capacityBack75=fs.readFileSync('backend/release-v75-capacity-utilization.mjs','utf8');
const capacity76=fs.readFileSync('frontend/release-capacity-readiness-v76.js','utf8');
const barcode77=fs.readFileSync('frontend/barcode-workflow-v77.js','utf8');
const barcodeBack77=fs.readFileSync('backend/release-v77-barcode-resolver.mjs','utf8');
const worker=fs.readFileSync('backend/worker-production.mjs','utf8');
const errors=[];
const need=(ok,msg)=>{if(!ok)errors.push(msg);};
const handlers={dashboard:'dashboard',documents:'documents',quality:'quality',operations:'operations',users:'users',settings:'settings',audit:'audit',import:'importCenter',shopfloor:'shopfloor',live:'liveMachines',integrations:'integrations'};
const defined=name=>new RegExp(`function\\s+${name}\\s*\\(`).test(bundle)||new RegExp(`(?:window\\.)?${name}\\s*=\\s*(?:async\\s*)?function\\s*\\(`).test(bundle)||new RegExp(`(?:window\\.)?${name}\\s*=\\s*(?:async\\s*)?\\(`).test(bundle);
for(const [view,handler] of Object.entries(handlers)){need(workspace.includes(`view==='${view}'`)||workspace.includes(`view === '${view}'`),`Render route ${view} tidak ditemukan.`);need(defined(handler),`Handler ${handler} untuk route ${view} tidak ditemukan pada bundle aktif.`);}
need(workspace.includes("view.startsWith('dept:')"),'Render route department dinamis tidak ditemukan.');need(defined('departmentHome'),'Handler departmentHome tidak ditemukan.');
need(workspace.includes("nav('dashboard'"),'Menu Beranda hilang.');need(workspace.includes("nav('shopfloor'"),'Menu HMI produksi hilang.');need(workspace.includes("nav('live'"),'Menu Status mesin hilang.');need(workspace.includes("nav('documents'"),'Menu Dokumen & aset hilang.');need(workspace.includes("nav('quality'"),'Menu Validasi sumber hilang.');need(workspace.includes("user.role==='superadmin'"),'Guard menu Superadmin hilang.');need(workspace.includes("can(user.department,'config')"),'Guard menu Konfigurasi hilang.');
const releaseRoutes=[['governance',"view==='governance'",'renderGovernance','data-view="governance"'],['data-governance',"view==='data-governance'",'DG16View?.render','data-view="data-governance"'],['operational-control',"view==='operational-control'",'OC31View?.render','data-view="operational-control"'],['uat-release',"view==='uat-release'",'UAT17View?.render','data-view="uat-release"'],['support-recovery',"view==='support-recovery'",'supportRecovery','data-view="support-recovery"'],['approvals',"view==='approvals'",'approvalWorkspace','data-view="approvals"']];
for(const [view,routeMarker,handlerMarker,menuMarker] of releaseRoutes){need(bundle.includes(routeMarker),`Route release-control ${view} tidak ditemukan.`);need(bundle.includes(handlerMarker),`Handler release-control ${view} tidak ditemukan.`);need(bundle.includes(menuMarker),`Menu release-control ${view} tidak ditemukan.`);}
need(bundle.includes("view==='departments'")&&bundle.includes('departmentHub64')&&bundle.includes("hub.dataset.view='departments'"),'Department Hub route/menu tidak lengkap.');
need(bundle.includes("user?.role==='superadmin'&&!nav.querySelector('[data-view=\"governance\"]')"),'Tata Kelola & Readiness tidak dijaga untuk Superadmin.');
need(bundle.includes("if(user?.role!=='superadmin')return")&&bundle.includes('data-view="support-recovery"'),'Support & Recovery tidak dijaga untuk Superadmin.');
need(index.includes('role-ux-v7.js'),'Role UX guard tidak dimuat.');need(index.includes('asset-repair-v9.js'),'BMJ logo/hero runtime tidak dimuat.');need(index.includes('field-display-v8.js'),'Field display runtime tidak dimuat.');need(index.includes('data-governance-core-v16.js'),'Data Governance runtime tidak dimuat.');need(index.includes('operational-control-core-v31.js'),'Operational Control runtime tidak dimuat.');need(index.includes('uat-release-core-v17.js'),'UAT & Go-Live runtime tidak dimuat.');need(index.includes('support-recovery-v21.js'),'Support & Recovery runtime tidak dimuat.');
need(index.includes('form-semantics-v65.js')&&bundle.includes('Arsipkan transaksi')&&bundle.includes('Satuan wajib'),'Form Semantics v65 tidak aktif/lengkap.');need(index.includes('page-integrity-v66.js')&&bundle.includes('__RESELECT_REQUIRED__')&&bundle.includes('decorateApprovalHistory'),'Page Integrity v66 tidak aktif/lengkap.');need(index.includes('live-page-integrity-v67.js')&&bundle.includes('telemetry_trusted')&&bundle.includes('counter/speed disembunyikan'),'Live Page Integrity v67 tidak aktif/lengkap.');need(index.includes('dashboard-role-depth-v68.js')&&bundle.includes('Role Depth v68')&&bundle.includes('Definisi, authority & periode KPI'),'Dashboard Role Depth v68 tidak aktif/lengkap.');
need(displayFront.includes('function machineAuthority')&&displayFront.includes('machineCanonical')&&displayFront.includes("ready:!!name&&!!machine&&authority.canonical")&&displayBack.includes('canonicalMachineProblem')&&displayBack.includes('Machine assignment harus memakai canonical machine yang sudah disahkan pada Data Governance'),'Display publish canonical-machine guard tidak lengkap.');
need(index.includes('integration-safety-v69.js')&&bundle.includes('PRODUCTION CONNECTION POLICY')&&bundle.includes('Worker Secret')&&bundle.includes('private/link-local address'),'Integration Safety v69 tidak aktif/lengkap.');
need(index.includes('navigation-integrity-v70.js')&&bundle.includes('Operational Inbox')&&bundle.includes('Sesi hanya disimpan untuk sesi browser ini.')&&bundle.includes('Jaringan online')&&bundle.includes('Cari menu atau sumber data'),'Navigation Integrity v70 tidak aktif/lengkap.');
need(index.includes('live-monitoring-v71.js')&&live71.includes('data-machine-code')&&live71.includes('telemetryMap')&&live71.includes("byCode.get(norm(m.code))")&&live71.includes("Promise.all([api('/realtime/overview'),api('/telemetry-status')])")&&!live71.includes('rows[i]'),'Live Monitoring v71 harus mengikat telemetry berdasarkan machine code, bukan posisi kartu.');
need(index.includes('hmi-quality-v72.js')&&quality72.includes('name="unit" required')&&quality72.includes("api('/shopfloor/quality','POST'")&&quality72.includes('tidak digabungkan dengan event bersatuan lain'),'HMI Quality v72 unit-safe tidak aktif/lengkap.');
need(qualityBack44.includes('Satuan Quality Event wajib diisi')&&qualityBack44.includes("GROUP BY COALESCE(NULLIF(lower(trim(unit)),''),'__missing__')")&&qualityBack44.includes('Event lama tanpa satuan'),'Backend Quality Unit v44 harus memisahkan agregasi berdasarkan satuan dan menandai legacy missing-unit.');
need(index.includes('process-capability-v73.js')&&process73.includes("api('/process-capability")&&process73.includes('ambiguous_specification')&&process73.includes('Data tidak digabungkan lintas unit'),'Process Capability v73 register-driven tidak aktif/lengkap.');
need(processBack59.includes("path==='/api/process-capability'")&&processBack59.includes('Ppk hanya dihitung untuk satu kombinasi mesin + parameter + satuan')&&processBack59.includes('ambiguous_specification')&&processBack59.includes('LIMIT 5001'),'Backend Process Capability v59 harus fail-safe terhadap unit/spec campuran dan subset terlalu besar.');
need(source41.includes('Inspeksi baris sumber')&&source41.includes('Reconciliation Register')&&source41.includes('Kolom tambahan · mode lanjutan'),'Source Inspector dan Reconciliation Register harus tetap business-facing.');
need(governance19.includes("['roles','devices','data','display','recovery','integrations']")&&governance19.includes('Final UAT menunggu gate UAT')&&governance19.includes('PIC dan evidence/alasan'),'Final UAT backend harus menunggu seluruh prerequisite gate beserta evidence.');
need(governance19.includes('utilizationAligned')&&governance19.includes('Order/Target/Planning dibanding Capacity/Kapasitas')&&governance19.includes('Final UAT menunggu definisi Utilization yang selaras dengan basis Order/Capacity'),'Data Governance dan Final UAT harus mengunci metode Utilization sesuai blueprint Order/Capacity.');
need(uatView.includes('6 prerequisite UAT selesai')&&uatView.includes('Dependency Final Sign-off')&&uatView.includes('Prerequisite lengkap'),'UAT view harus membedakan prerequisite dan final sign-off.');
need(uatEdit.includes('const prerequisites=')&&uatEdit.includes('Final Sign-off belum dapat diluluskan')&&uatEdit.includes('workflow/mirror consistency'),'UAT editor harus menahan final sign-off sebelum prerequisite selesai.');
need(['validateProduction','validateDowntime','validateMaintenance','validateChecklist','validateLogbook','validateMaster','validateConfirmation'].every(x=>entry63.includes(x))&&entry63.includes('Confirmation PPIC wajib memiliki')&&entry63.includes('Maintenance selesai/terverifikasi wajib memiliki tindakan korektif'),'Backend entry semantics harus menjaga completeness transaksi baru tanpa mengarang legacy.');
need(form65.includes('businessCompleteness')&&form65.includes('Wajib transaksi baru')&&form65.includes('Nomor konfirmasi bersama counter')&&form65.includes('Empat keputusan kesiapan harus eksplisit'),'Frontend form semantics harus mencerminkan business completeness backend.');
need(index.includes('support-evidence-v74.js')&&support74.includes('Cakupan Semantik Data')&&support74.includes('quality_units')&&support74.includes('production_units')&&support74.includes('pds_currency')&&support74.includes('energy')&&support74.includes('release_fingerprint'),'Support Evidence v74 harus menampilkan semantic data coverage dan release fingerprint.');
need(kpi45.includes('energy_records_30d')&&kpi45.includes('energy_kwh_30d')&&kpi45.includes("setMetric(metrics,'enpi'")&&kpi45.includes('Belum dihitung otomatis'),'Energy KPI v74 harus memakai tanggal transaksi/kWh valid dan tidak mengarang ENPI.');
need(support21.includes('async function energyCoverage')&&support21.includes('undated_or_invalid_date')&&support21.includes('ENPI tidak dihitung'),'Release manifest harus menyertakan evidence coverage Energy.');
need(index.includes('capacity-utilization-v75.js')&&capacity75.includes("api('/capacity-utilization")&&capacity75.includes('Tidak ada overall utilization')&&capacity75.includes('Planning dianalisis'),'Capacity Utilization v75 UI tidak aktif/lengkap.');
need(capacityBack75.includes("url.pathname!=='/api/capacity-utilization'")&&capacityBack75.includes('definitionAligned')&&capacityBack75.includes("IN ('Released','Dimulai')")&&capacityBack75.includes('Agregasi hanya di dalam unit yang sama')&&capacityBack75.includes('Target Order / kapasitas shift'),'Backend Capacity Utilization v75 harus fail-closed, Released/Dimulai only, dan unit-safe.');
need(index.includes('release-capacity-readiness-v76.js')&&capacity76.includes('Capacity & Utilization Authority')&&capacity76.includes("d.ready===true&&d.authoritative===true")&&capacity76.includes("badge.textContent='Belum final'"),'Release Capacity v76 harus aktif dan menurunkan status release saat authority tidak siap.');
need(index.includes('barcode-workflow-v77.js')&&barcode77.includes("api('/barcode/resolve?value=")&&barcode77.includes('exact match only')&&barcode77.includes('Scan tidak melakukan Start otomatis')&&barcode77.includes('v77ResolvedPlan')&&barcode77.includes('stopImmediatePropagation'),'Barcode Workflow v77 harus exact-resolve, tidak auto-start, dan memblokir submit scan yang belum tervalidasi.');
need(barcodeBack77.includes("url.pathname!=='/api/barcode/resolve'")&&barcodeBack77.includes("json_extract(payload,'$.status')='Released'")&&barcodeBack77.includes("status:'ambiguous'")&&barcodeBack77.includes("status:'machine_mismatch'")&&barcodeBack77.includes('Exact match only')&&!barcodeBack77.includes('includes(raw)'),'Backend Barcode Resolver v77 harus exact-only, Released-only, machine-scoped, dan fail-safe terhadap ambiguity.');
need(worker.includes('handleBarcodeResolverV77')&&worker.includes('barcode-resolver-v77'),'Worker harus memuat endpoint dan fingerprint Barcode Resolver v77.');
need(worker.includes('handleCapacityUtilizationV75')&&worker.includes('capacity-utilization-v75'),'Worker harus memuat endpoint dan fingerprint Capacity Utilization v75.');
need(worker.includes('afterWorkCalendarStartV62(workCalendarSignal')&&!worker.includes('workCalendarStartSignal'),'Work Calendar start signal wiring harus memakai signal yang benar.');
if(errors.length){console.error('Navigation validation FAILED');for(const e of errors)console.error('- '+e);process.exit(1);}
console.log(`Navigation validation OK — ${Object.keys(handlers).length+1+releaseRoutes.length+1} routes + critical guards checked across ${active.length} active JS bundles.`);
