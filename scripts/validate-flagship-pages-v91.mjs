import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8');
const index=read('frontend/index.html');
const css=read('frontend/flagship-pages-v91.css');
const finalCss=read('frontend/flagship-final-v92.css');
const js=read('frontend/flagship-pages-v91.js');
const nav=read('scripts/validate-navigation.mjs');
const routes=['dashboard','departments','documents','quality','operations','live','shopfloor','approvals','governance','data-governance','operational-control','uat-release','support-recovery','settings','users','audit','integrations','import'];
const need=[];
const check=(name,ok)=>need.push([name,!!ok]);
check('v91 CSS active',index.includes('flagship-pages-v91.css?v=20260917-1'));
check('v91 JS active',index.includes('flagship-pages-v91.js?v=20260917-1'));
check('v92 final CSS active',index.includes('flagship-final-v92.css?v=20260917-1'));
check('visual authority order',index.indexOf('flagship-final-v92.css')>index.indexOf('flagship-pages-v91.css')&&index.indexOf('flagship-pages-v91.css')>index.indexOf('flagship-depth-v90.css')&&index.indexOf('flagship-depth-v90.css')>index.indexOf('flagship-v89.css'));
check('v91 is final JS authority',index.indexOf('flagship-pages-v91.js')>index.indexOf('flagship-depth-v90.js')&&index.indexOf('flagship-depth-v90.js')>index.indexOf('flagship-v89.js'));
check('all named routes have runtime identity',routes.every(r=>js.includes(`${r}:`)||js.includes(`'${r}':`)));
check('dynamic department route has identity',js.includes("v.startsWith('dept:')")&&js.includes('Department Workspace'));
check('department hub spatial layout',css.includes('data-ui-view="departments"')&&css.includes('.ref-depts>article:nth-child(1)'));
check('department workspace spatial layout',css.includes('data-ui-view^="dept:"')&&css.includes('.v36-dept-summary'));
check('documents spatial layout',css.includes('data-ui-view="documents"')&&css.includes('.v36-source-filters')&&css.includes('.source-registry'));
check('quality spatial layout',css.includes('data-ui-view="quality"')&&css.includes('.v36-quality-path')&&css.includes('.v41-owner-quality'));
check('operations spatial layout',css.includes('data-ui-view="operations"')&&css.includes('.module-table-v25')&&css.includes('.v36-register-guide'));
check('live cockpit spatial layout',css.includes('data-ui-view="live"')&&css.includes('.machine-live-card')&&css.includes('.v36-live-summary'));
check('HMI cockpit spatial layout',css.includes('data-ui-view="shopfloor"')&&css.includes('.run-hero')&&css.includes('.hmi-side'));
check('approval decision desk layout',css.includes('data-ui-view="approvals"')&&css.includes('.approval-kpis')&&css.includes('.v36-approval-filter'));
check('governance control center family',['governance','data-governance','operational-control','uat-release','support-recovery'].every(r=>css.includes(`data-ui-view="${r}"`))&&css.includes('.oc31-section'));
check('admin console family',['settings','users','audit','integrations','import'].every(r=>css.includes(`data-ui-view="${r}"`))&&css.includes('.integration-card')&&css.includes('.ic30-flow'));
check('route masthead is semantic only',js.includes('flagship-routebar')&&js.includes('replaceChildren')&&!/\b(api\(|fetch\(|localStorage|sessionStorage|\/api\/)/.test(js));
check('responsive route layouts',css.includes('@media(max-width:1220px)')&&css.includes('@media(max-width:820px)')&&css.includes('@media(max-width:520px)'));
check('final CSS uses fluid sizing',finalCss.includes('clamp(')&&finalCss.includes('repeat(auto-fit,minmax('));
check('final CSS prevents text collisions',finalCss.includes('min-width:0')&&finalCss.includes('overflow-wrap:anywhere')&&finalCss.includes('text-wrap:balance'));
check('final CSS protects dark contrast',finalCss.includes('.sidebar')&&finalCss.includes('.run-hero')&&finalCss.includes('.oc31-banner')&&finalCss.includes('color:#f4fbff!important'));
check('deep storage telemetry treatment',finalCss.includes('.v56-grid')&&finalCss.includes('.v56-meter'));
check('deep barcode workflow treatment',finalCss.includes('.barcode-v77-status')&&finalCss.includes('.barcode-v77-policy')&&finalCss.includes('#barcodePlanScan'));
check('deep OEE finish treatment',finalCss.includes('#v81Finish')&&finalCss.includes('.v81-approval-oee'));
check('attention severity contrast',finalCss.includes('.attention-self[data-state="critical"]')&&finalCss.includes('.attention-self[data-state="active"]'));
check('motion accessibility preserved',finalCss.includes('@media(prefers-reduced-motion:reduce)'));
check('navigation validator still covers route set',['dashboard','documents','quality','operations','users','settings','audit','import','shopfloor','live','integrations'].every(r=>nav.includes(`${r}:`))&&nav.includes("['governance'")&&nav.includes("['approvals'"));
check('no prototype language',!(/\b(prototype|mockup|dummy|lorem ipsum|data demo)\b/i.test(css+'\n'+finalCss+'\n'+js)));
const failed=need.filter(([,ok])=>!ok);if(failed.length){for(const [name] of failed)console.error('FAIL:',name);process.exit(1);}console.log(`Flagship v91/v92 validation OK — ${need.length} route, contrast, fluid-layout, and deep-component guards checked.`);
