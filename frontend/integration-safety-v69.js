/* OEE Collaboraction Integration Safety v69 — visible production policy matching backend guard. */
(()=>{
 const policyHtml=`<section class="panel v69-integration-policy"><div class="release-section-head"><div><span class="eyebrow">PRODUCTION CONNECTION POLICY</span><h2>Aturan koneksi yang dapat dirilis</h2><p>Credential aktual tidak pernah disimpan pada browser atau D1. Form hanya menyimpan nama Worker Secret dan metadata koneksi.</p></div><span class="release-status ok">Backend guard aktif</span></div><div class="v69-policy-grid"><div><strong>REST / ODATA</strong><span>Endpoint production harus HTTPS publik yang dapat diverifikasi. Localhost, private/link-local address, dan credential di URL ditolak.</span></div><div><strong>EDGE_PUSH</strong><span>Untuk event mesin/PLC melalui secure edge gateway. Tidak memakai Base URL pada konfigurasi Worker.</span></div><div><strong>PULL_FEED</strong><span>Untuk feed analytics keluar. Tidak memakai Base URL pada konfigurasi Worker.</span></div><div><strong>Worker Secret</strong><span>Isi hanya nama environment variable, contoh SAP_API_TOKEN. Nilai token/credential dipasang sebagai Cloudflare Worker Secret.</span></div><div><strong>Polling</strong><span>1–1440 menit. Scheduled sync hanya berlaku pada koneksi pull REST/ODATA yang diaktifkan.</span></div><div><strong>Target</strong><span>machine_events, planning, atau analytics_feed sesuai mode. Mapping dibatasi dan divalidasi sebelum disimpan/test.</span></div></div></section>`;
 function enhanceFields(root=document){
  const base=root.querySelector?.('[name="base_url"]');if(base){base.type='url';base.placeholder='https://integration.example.com/api/...';base.autocomplete='url';base.title='Production REST/ODATA wajib menggunakan HTTPS publik. Credential tidak boleh ditanam pada URL.';}
  const secret=root.querySelector?.('[name="secret_env"]');if(secret){secret.pattern='[A-Z][A-Z0-9_]{1,63}';secret.placeholder='SAP_API_TOKEN';secret.autocomplete='off';secret.title='Masukkan nama Cloudflare Worker Secret, bukan nilai token.';}
  const poll=root.querySelector?.('[name="poll_minutes"]');if(poll){poll.type='number';poll.min='1';poll.max='1440';poll.step='1';}
  const mode=root.querySelector?.('[name="mode"]');if(mode){mode.title='Mode didukung: REST, ODATA, EDGE_PUSH, PULL_FEED';}
  const config=root.querySelector?.('[name="config"]');if(config){config.setAttribute('spellcheck','false');config.title='Config divalidasi backend. Target: machine_events, planning, atau analytics_feed.';}
 }
 function paint(){if(view!=='integrations')return;const root=$('#content');if(!root)return;if(!root.querySelector('.v69-integration-policy')){const h=root.querySelector('.heading'),summary=root.querySelector('.v36-integration-summary');(summary||h)?.insertAdjacentHTML('afterend',policyHtml);}enhanceFields(root);}
 const baseRender=typeof render==='function'?render:null;if(baseRender){render=async function(...args){const out=await baseRender(...args);queueMicrotask(paint);return out;};}
 document.addEventListener('click',()=>{if(view==='integrations')setTimeout(()=>enhanceFields(document.querySelector('#modal')||document),0);},true);
 window.IntegrationSafetyV69={paint,enhanceFields};
})();
