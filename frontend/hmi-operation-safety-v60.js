/* BMJ OEE HMI Operation Safety v60 — fail closed on machine identity, telemetry freshness, and governed work-calendar context. */
(()=>{
 const SENTINEL='__RESELECT_REQUIRED__';
 const norm=v=>String(v||'').trim().toUpperCase().replace(/[^A-Z0-9]/g,'');
 const normShift=v=>{const s=String(v||'').trim().toUpperCase().replace(/\s+/g,'');const d=s.match(/(?:SHIFT|S)?([123])$/)?.[1];if(d)return d;if(['I','II','III'].includes(s))return String(['I','II','III'].indexOf(s)+1);return '';};
 const normGroup=v=>{const s=String(v||'').trim().toUpperCase().replace(/\s+/g,'');const g=s.match(/(?:GROUP|GRUP|G)?([ABCD])$/)?.[1];return g||'';};
 const planDate=v=>String(v||'').trim().match(/^(\d{4}-\d{2}-\d{2})/)?.[1]||'';
 let telemetry={generated_at:null,freshness_seconds:180,machines:[]},workCalendar={approved:false,runtime_ready:false},calendarLoadedAt=0,latestPlans=[];
 const telemetryRow=code=>(telemetry.machines||[]).find(x=>norm(x.code)===norm(code));
 async function loadTelemetry(){try{telemetry=await api('/telemetry-status');}catch{telemetry={generated_at:null,freshness_seconds:180,machines:[]};}return telemetry;}
 async function loadWorkCalendar(force=false){if(!force&&Date.now()-calendarLoadedAt<30000)return workCalendar;try{workCalendar=await api('/work-calendar/context');calendarLoadedAt=Date.now();}catch{workCalendar={approved:false,runtime_ready:false,reason:'Konteks kalender kerja belum dapat dimuat'};}return workCalendar;}
 function formatOperationalDateTime(ts){const d=ts instanceof Date?ts:new Date(ts);if(Number.isNaN(d.getTime()))return '—';if(workCalendar.runtime_ready&&workCalendar.timezone){try{return d.toLocaleString('id-ID',{timeZone:workCalendar.timezone,dateStyle:'medium',timeStyle:'medium'});}catch{}}return d.toLocaleString('id-ID')+' · waktu browser';}
 function paintOperationalClock(){const clock=document.querySelector('.live-clock');if(!clock)return;const strong=clock.querySelector('strong'),span=clock.querySelector('span'),d=new Date();if(workCalendar.runtime_ready&&workCalendar.timezone){try{if(strong)strong.textContent=d.toLocaleTimeString('id-ID',{timeZone:workCalendar.timezone,hour:'2-digit',minute:'2-digit',second:'2-digit'});if(span)span.textContent=d.toLocaleDateString('id-ID',{timeZone:workCalendar.timezone,weekday:'long',day:'2-digit',month:'long',year:'numeric'});clock.title=`Waktu operasional · ${workCalendar.timezone}`;clock.dataset.timeAuthority='work-calendar';return;}catch{}}clock.title='Waktu perangkat/browser · Work Calendar belum runtime-ready';clock.dataset.timeAuthority='browser';if(span&&!/browser/i.test(span.textContent))span.textContent+=' · browser';}
 function blockForReselect(requested,fallback){
  if(view!=='shopfloor')return;hmiMachine=SENTINEL;
  document.querySelectorAll('[data-machine]').forEach(b=>{b.classList.remove('active');b.setAttribute('aria-pressed','false');});
  const headingEl=document.querySelector('#content .heading');let notice=document.querySelector('.v60-machine-reselect');if(!notice){notice=document.createElement('div');notice.className='notice v60-machine-reselect';headingEl?.insertAdjacentElement('afterend',notice);}
  notice.textContent=`Mesin ${requested||'yang dipilih'} tidak lagi tersedia pada overview. ${fallback?`Data fallback ${fallback} hanya ditampilkan untuk konteks dan tidak boleh dipakai untuk transaksi. `:''}Pilih mesin secara eksplisit sebelum menjalankan Start PRO, Downtime, Quality, Maintenance, atau Finish.`;
  document.querySelectorAll('#startRun input,#startRun select,#startRun button,.hmi-actions button,#stopDown,#callMtc,#ackMtc').forEach(el=>{el.disabled=true;el.setAttribute('aria-disabled','true');});
  const main=document.querySelector('.hmi-main');if(main)main.setAttribute('data-operation-locked','machine-reselect');
 }
 function machineExists(code){const key=norm(code);return !!key&&[...document.querySelectorAll('[data-machine]')].some(b=>norm(b.dataset.machine)===key);}
 function freshnessText(s){if(!s)return 'Telemetry machine belum terdaftar';if(!s.heartbeat_at)return `Heartbeat belum tersedia · source ${s.source_type||'belum diketahui'}`;const age=Number(s.heartbeat_age_seconds);return `${s.telemetry_trusted?'Telemetry fresh':'Telemetry tidak authoritative'} · ${Number.isFinite(age)?age+' detik sejak heartbeat':'umur heartbeat tidak tersedia'} · ${s.source_type||'source belum diketahui'}`;}
 function bindPlanningCalendarGate(plans){
  const form=$('#startRun'),choice=$('#planChoice');if(!form||!choice||choice.dataset.v62Gate)return;choice.dataset.v62Gate='1';choice.required=true;const shift=form.elements.shift,group=form.elements.group,start=form.querySelector('.hmi-start');let gate=document.querySelector('.v62-plan-calendar-gate');if(!gate){gate=document.createElement('div');gate.className='notice v62-plan-calendar-gate';const checklist=form.querySelector('.checklist-gate');checklist?.insertAdjacentElement('beforebegin',gate);}
  const releaseCalendarBlock=blocked=>{if(!start)return;if(blocked){start.dataset.calendarBlocked='1';start.disabled=true;}else if(start.dataset.calendarBlocked==='1'&&!document.querySelector('.hmi-main[data-operation-locked]')){delete start.dataset.calendarBlocked;start.disabled=false;}};
  const draw=()=>{const selected=(plans||[]).find(x=>String(x.row?.id)===String(choice.value)),plan=selected?.payload||null;if(!plan){gate.textContent='Pilih Planning Released. Tanggal kerja, Shift, dan Group akan diselaraskan sebelum Start PRO.';gate.className='notice v62-plan-calendar-gate';releaseCalendarBlock(false);return;}const ps=normShift(plan.shift),pg=normGroup(plan.group),pd=planDate(plan.work_date||plan.date);let blockers=[];if(plan.shift&&!ps)blockers.push(`format Shift planning tidak dikenali (${plan.shift})`);if(plan.group&&!pg)blockers.push(`format Group planning tidak dikenali (${plan.group})`);if(group&&pg&&[...group.options].some(o=>String(o.value).toUpperCase()===pg))group.value=pg;if(workCalendar.runtime_ready&&workCalendar.shift){if(!pd)blockers.push('tanggal kerja/eksekusi planning belum valid');else if(pd!==workCalendar.work_date)blockers.push(`tanggal planning ${pd} berbeda dari tanggal kerja ${workCalendar.work_date}`);if(shift&&[...shift.options].some(o=>String(o.value)===String(workCalendar.shift)))shift.value=String(workCalendar.shift);if(ps&&ps!==String(workCalendar.shift))blockers.push(`Planning Shift ${ps} berbeda dari Shift runtime ${workCalendar.shift}`);}else if(shift&&ps&&[...shift.options].some(o=>String(o.value)===ps))shift.value=ps;if(blockers.length){gate.className='errorbox v62-plan-calendar-gate';gate.textContent='Start PRO ditahan: '+blockers.join(' · ')+'. Minta PPIC/owner kalender memperbaiki baseline sebelum eksekusi.';releaseCalendarBlock(true);}else{gate.className='notice v62-plan-calendar-gate';gate.textContent=`Planning sinkron · ${pd?'tanggal '+pd+' · ':''}Shift ${shift?.value||'—'} · Group ${group?.value||'—'}${workCalendar.runtime_ready?' · tanggal kerja '+(workCalendar.work_date||'—'):' · Work Calendar belum authoritative'}.`;releaseCalendarBlock(false);}};
  choice.addEventListener('change',draw);draw();
 }
 function paintWorkCalendar(){
  if(view!=='shopfloor')return;document.querySelector('.v62-work-calendar')?.remove();const headingEl=document.querySelector('#content .heading'),anchor=document.querySelector('.v36-hmi-context')||headingEl;if(!anchor)return;
  const box=document.createElement('div');box.className=workCalendar.runtime_ready?'v36-hmi-context v62-work-calendar':'notice v62-work-calendar';
  if(workCalendar.runtime_ready){box.innerHTML=`<div><span>Tanggal kerja</span><strong>${esc(workCalendar.work_date||'—')}</strong></div><div><span>Shift aktif</span><strong>${workCalendar.shift?`Shift ${esc(workCalendar.shift)}`:'Di luar window'}</strong></div><div><span>Zona waktu</span><strong>${esc(workCalendar.timezone||'—')}</strong></div><div><span>Cut-off</span><strong>${esc(workCalendar.workday_cutoff||'—')}</strong><small>${esc(workCalendar.group_model||'model group belum dicatat')}</small></div>`;const shift=document.querySelector('#startRun [name="shift"]');if(shift&&workCalendar.shift&&[...shift.options].some(o=>String(o.value)===String(workCalendar.shift))){shift.value=String(workCalendar.shift);shift.title='Shift mengikuti kalender kerja authoritative. Backend akan menolak planning yang tidak cocok.';}}
  else box.textContent=workCalendar.approved?`Kalender shift sudah disahkan tetapi belum siap dipakai runtime: ${workCalendar.reason||'timezone/window belum valid'}. Start PRO belum memakai kalender ini sampai baseline diperbaiki.`:'Kalender shift & tanggal kerja belum authoritative. Start PRO masih mengikuti planning; selesaikan Data Governance sebelum final operational sign-off.';
  anchor.insertAdjacentElement('afterend',box);paintOperationalClock();
 }
 function maskSelectedTelemetry(){
  if(view!=='shopfloor'||hmiMachine===SENTINEL)return;const s=telemetryRow(hmiMachine),trusted=!!s?.telemetry_trusted,live=document.querySelector('.live-metrics');
  document.querySelector('.v60-telemetry-warning')?.remove();if(trusted)return;
  const counter=live?.querySelector('.metric-big strong'),speed=live?.querySelector('.metric-pair>div:first-child strong');if(counter){counter.textContent='—';counter.title='Counter disembunyikan karena telemetry tidak fresh/authoritative.';}if(speed){speed.textContent='—';speed.title='Speed disembunyikan karena telemetry tidak fresh/authoritative.';}
  if(s?.run_id&&!s.auto_counter_finish_ready){const actual=document.querySelector('.run-kpis>div:nth-child(2) strong');if(actual){actual.textContent='—';actual.title='Actual live dari counter tidak authoritative untuk run ini.';}}
  const liveAnchor=live||document.querySelector('.hmi-side'),notice=document.createElement('div');notice.className='notice v60-telemetry-warning';notice.textContent=`${freshnessText(s)}. Counter/speed yang tidak fresh ditampilkan sebagai —.${s?.run_id&&!s.auto_counter_finish_ready?' Finish PRO wajib memakai Actual Qty manual karena lineage counter awal/akhir tidak memenuhi syarat.':''}`;liveAnchor?.insertAdjacentElement('afterend',notice);
 }
 function maskMachineWall(){
  if(view!=='live')return;document.querySelectorAll('.machine-live-card').forEach(card=>{const code=card.querySelector('.machine-live-head small')?.textContent?.trim(),s=telemetryRow(code);if(s?.telemetry_trusted)return;const values=card.querySelectorAll('.machine-live-body>div strong');if(values[1])values[1].textContent='—';if(values[2])values[2].textContent='—';const foot=card.querySelector('.machine-live-foot');if(foot){foot.classList.add('warn');foot.textContent=`${freshnessText(s)} · counter/speed tidak ditampilkan`;}});
 }
 function decorateFinish(run){
  const form=$('#v40Finish');if(!form)return;const s=(telemetry.machines||[]).find(x=>String(x.run_id||'')===String(run?.id||''));if(s?.auto_counter_finish_ready)return;const actual=form.elements.actual_qty;if(actual){actual.required=true;actual.placeholder='Wajib diisi manual';const label=actual.closest('label'),small=label?.querySelector('small');if(small)small.textContent='wajib manual · counter tidak authoritative';}
  const balance=$('#v40FinishBalance'),n=document.createElement('div');n.className='full notice v60-finish-manual';n.textContent=`Actual Qty manual wajib diisi. ${freshnessText(s)}${s&&!s.counter_start_trusted?' · counter awal run tidak trusted':''}.`;balance?.insertAdjacentElement('beforebegin',n);
  const baseDraw=()=>{const raw=String(actual?.value||'').trim(),save=$('#v40FinishSave');if(save&&raw==='')save.disabled=true;};actual?.addEventListener('input',baseDraw);baseDraw();
 }
 if(typeof finishDialog==='function'){const baseFinishV60=finishDialog;finishDialog=function(run){const out=baseFinishV60(run);queueMicrotask(()=>decorateFinish(run));return out;};}
 if(typeof bindHmiActions==='function'){const baseBindV60=bindHmiActions;bindHmiActions=function(active,m,down,call,plans){latestPlans=Array.isArray(plans)?plans:[];return baseBindV60(active,m,down,call,plans);};}
 if(typeof shopfloor==='function'){
  const baseShopfloorV60=shopfloor;
  shopfloor=async function(...args){const requested=String(hmiMachine||''),explicitBefore=!!requested&&requested!==SENTINEL,out=await baseShopfloorV60(...args),fallback=String(hmiMachine||'');await Promise.all([loadTelemetry(),loadWorkCalendar()]);if(requested===SENTINEL||explicitBefore&&!machineExists(requested)||explicitBefore&&norm(fallback)!==norm(requested))blockForReselect(requested===SENTINEL?'mesin sebelumnya':requested,fallback===SENTINEL?'':fallback);else maskSelectedTelemetry();paintWorkCalendar();bindPlanningCalendarGate(latestPlans);return out;};
 }
 if(typeof liveMachines==='function'){const baseLiveV60=liveMachines;liveMachines=async function(...args){const out=await baseLiveV60(...args);await loadTelemetry();maskMachineWall();return out;};}
 window.HMIOperationSafetyV60={blockForReselect,machineExists,loadTelemetry,loadWorkCalendar,bindPlanningCalendarGate,paintWorkCalendar,paintOperationalClock,formatOperationalDateTime,maskSelectedTelemetry,maskMachineWall,get telemetry(){return telemetry;},get workCalendar(){return workCalendar;}};
})();
